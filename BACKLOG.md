# Survivor 项目 Backlog

更新时间：2026-09-22

本文只管理当前主要演进模块 Survivor 的未完成工作、优先级和验收出口。项目现状以代码、场景/资源和 `Docs/Modules` 为准；已完成条目只在文末保留简要索引，不与待办混排。

优先级：

- **P0**：阻塞核心流程、稳定性或下一阶段交付。
- **P1**：改善现有玩法质量、可观测性和可维护性。
- **P2**：扩展玩法内容与长期成长。
- **P3**：自动化、性能基线与发布准备。

状态只使用 `待实现`、`进行中`、`已完成`、`已取消`。条目只有在代码/资源、文档和验收证据都闭合后才能标记为已完成。

## 当前焦点

### SB-006 UI 自适应与视觉比例校准

**优先级：P0**　**状态：待实现**　**依赖：SB-002**

目标：以当前竖屏参考分辨率 `720×1280` 为基线，让 Survivor Home、HUD、三选一和 GameOver 在目标比例与安全区域内保持可读、无遮挡且视觉比例一致。

实施范围：

- 明确 CanvasScaler 的参考分辨率、Screen Match Mode 和 Match 参数。
- 为安全区域建立单一适配入口，避免每个面板各写一套屏幕边距逻辑。
- 校准血条、经验条、货币、升级卡和 GameOver 的锚点、边距与缩放。
- 对照玩家、敌人、掉落物和相机 Size 验证场景视觉比例。
- 优先调整目标 Prefab 的布局约束，不用整体放大 Canvas 或随意提高 sorting order 掩盖问题。

完成定义：

- [ ] 至少覆盖 `9:16`、`9:19.5` 和一个带安全区域的移动端比例。
- [ ] Home → 战斗 → 升级 → GameOver → 返回/重开的完整流程无裁切、重叠或误触。
- [ ] 记录 CanvasScaler、安全区域入口、目标分辨率和必要截图。
- [ ] 同步 `SurvivorSceneFlow.md`、视觉规范或相关 Prefab 配置说明。

## P1：现有玩法质量

### SB-007 敌人、掉落与对象池运行时监控

**状态：待实现**　**依赖：SB-002**

提供仅开发环境启用的轻量统计：当前 Wave/GameTime、玩家等级、武器等级、活跃敌人/掉落/武器效果数量，以及各池缓存数量。统计更新应节流，不在高频路径使用 LINQ、字符串持续拼接或每帧扫描整个场景。

完成定义：

- [ ] 可在 Inspector 或开发开关中启停，正式构建默认关闭。
- [ ] 统计值能辅助确认生成上限、超距回收、重开清理和池缩容。
- [ ] Profiler 中关闭监控时无额外持续分配，开启时更新频率和开销有记录。

### SB-015 资源异步入口与所有权闭环

**状态：待实现**　**依赖：无**

按 `Docs/ResourceManagement.md` 推进一条正式 UI 异步加载链路，处理重复打开、加载失败、界面/Module 提前关闭和句柄释放。不得让 Addressables API 或 `AsyncOperationHandle` 泄漏到业务层。

完成定义：

- [ ] 同一资源的并发请求可合并或有明确的唯一加载所有者。
- [ ] 调用方销毁后，异步完成不会创建悬空 View。
- [ ] 至少一个正式 UI 流程完成 Play Mode 验收。
- [ ] `Docs/ResourceManagement.md` 与 `Docs/Architecture.md` 同步更新。

### SB-016 Network 收包边界保护

**状态：进行中（代码与编译检查完成，待 Play Mode 异常包验收）**　**依赖：无**

为单包处理增加最大包体、Packet/Proto/Handler 异常隔离和包含 cmd/长度/阶段的诊断信息。异常包不得让后续消息处理或连接状态机崩溃。详细边界见 `Assets/Scripts/Framework/Network/README.md`。

当前实现：入站帧默认限制为 1 MiB；空包/短包/超限包、未知 cmd、Proto 解码失败和 Handler 异常都会在独立阶段终止当前帧，且日志不包含协议正文。仓库暂无正式 Test Assembly，仍需在 Unity Play Mode 注入各类异常帧并确认后续合法帧可继续处理。

完成定义：

- [ ] 非法长度、未知 cmd、Proto 解码失败和业务 Handler 异常均有独立验证。
- [ ] 单包失败后可以继续处理后续合法消息。
- [x] 不记录完整敏感协议内容。

## P2：玩法内容扩展

### SB-008 Wave 难度和事件扩展

**状态：规划**　**依赖：SB-005、SB-007**

- 敌人属性配置或等价的稳定数据入口。
- 敌人预算、Spawn Weight 和可解释的动态难度。
- Elite/Boss、特殊出生规则、Wave 奖励、完成条件和胜利状态。
- 继续复用现有 `PoolManager`，不创建第二套敌人池。

### SB-009 Coin 局外成长与持久化

**状态：规划**　**依赖：SB-002**

- 定义本局 Coin 结算到局外数据的边界。
- 增加可迁移、可失败恢复的本地存档。
- 增加局外升级入口、消费规则和新一局读取流程。
- 运行时成长不得写回 `WeaponSO` 等源资源。

### SB-010 被动升级、武器进化和构筑系统

**状态：规划**　**依赖：SB-004、SB-009**

按小步增量接入被动属性、进化/合成条件、稀有度和构筑协同。新类型需复用或明确扩展 `UpgradeConfig`、`UpgradeId`、候选过滤和 `PlayerUpgradeContext`，避免产生第二套升级系统。

### SB-011 音效、特效和反馈

**状态：规划**　**依赖：SB-002**

补充受击、死亡、拾取、升级、Wave 切换、GameOver 和重开的反馈；验证暂停、池复用和场景切换不会残留音效、特效或回调。

## P3：工程化与发布准备

### SB-012 Survivor 自动化测试

**状态：规划**　**依赖：SB-002**

优先为经验溢出、升级去重/过滤、Wave 时间区间和池对象状态重置增加 Edit Mode 测试；再用 Play Mode 测试覆盖 UI、碰撞、场景切换和 `Time.timeScale`。测试代码应放入明确的 Test Assembly，不能把 `Assets/Scripts/TestCode` 当作自动化测试。

### SB-013 场景和资源配置校验

**状态：规划**　**依赖：SB-002**

增加 Editor 或构建前校验：Wave 区间与敌人组件、WeaponSO ID/Prefab/等级/控制器映射、升级图标和 UI 资源键、Player/Enemy 标签与 2D 物理组件。

### SB-014 性能基线与构建验证

**状态：规划**　**依赖：SB-007**

在明确的目标设备与 Development Build 上记录敌人/投射物规模、帧时间、GC Alloc、池命中与长时间运行稳定性，并形成可重复的构建与采样步骤。

## 推荐顺序

```text
SB-006 UI 适配
    ↓
SB-007 运行时监控
    ├─→ SB-008 Wave 扩展
    └─→ SB-014 性能基线

SB-015 资源异步闭环 ─┐
SB-016 收包边界保护 ─┴→ 可与玩法线并行推进

SB-009 局外 Coin → SB-010 构筑系统
```

## 已完成基线

| 条目 | 结果 | 证据入口 |
| --- | --- | --- |
| SB-001 武器投射物与特效对象池 | Play Mode 已验收 | `Survivor.md`、`EnemySystem.md` |
| SB-002 核心玩法闭环 | Wave、掉落、升级、GameOver、连续重开已验收 | `PlayModeAcceptance.md` |
| SB-004 武器等级字段效果 | 各武器适用字段已落地并验收 | `UpgradeSystem.md` |
| SB-005 首版数值平衡 | 玩家、武器、敌人、Wave、掉落基线已验收 | `BalanceSystem.md` |

编号 `SB-003` 当前没有有效条目，保留空缺以避免重用历史编号。

## 变更同步规则

完成或调整条目时：

1. 同步对应的 `Docs/Modules/*.md` 和可复现验收记录。
2. 改变 Manager、Module 生命周期、UI 主关系、资源/网络主数据流时，同步 `Docs/Architecture.md`。
3. 新增序列化字段、Prefab、Scene 或 ScriptableObject 时，记录 Inspector 路径、默认值和兼容/迁移方式。
4. “代码已写”“静态检查通过”“Unity 编译通过”“Play Mode 验收通过”必须分开描述。
5. 新设想先进入待办；没有仓库证据时不能写入“已完成基线”。
